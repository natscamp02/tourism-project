<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') -%>
</head>

<body>
    <%- include('../partials/alerts') -%>

        <main class="flex">
            <%- include('../partials/sidebar') -%>

                <div class="container">
                    <h1>Edit a program</h1>

                    <form action="/programs/update" method="post">
                        <input type="hidden" name="program_id" value="<%= program.id %>">

                        <div>
                            <label for="title">Program Title</label>
                            <input type="text" name="title" id="title" value="<%= program.title %>" required>
                        </div>

                        <div>
                            <label for="description">Description</label>
                            <textarea name="description" id="description" cols="30" rows="5" required>
<%= program.description %>
                            </textarea>
                        </div>

                        <div>
                            <label for="duration">Duration</label>
                            <input type="text" name="duration" id="duration" value="<%= program.duration %>" required>
                        </div>

                        <div>
                            <label for="price">Price</label>
                            <input type="number" name="price" id="price" value="<%= program.price %>" required>
                        </div>

                        <button type="submit">Add program</button>
                    </form>

                </div>

        </main>

</body>

</html>